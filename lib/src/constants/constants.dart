import 'package:flutter/material.dart';

const kDefaultBorderRadiusSmall = BorderRadius.all(Radius.circular(6.0));
const kDefaultBorderRadiusMedium = BorderRadius.all(Radius.circular(8.0));
const kDefaultBorderRadiusLarge = BorderRadius.all(Radius.circular(12.0));
const kDefaultBorderRadiusExtraLarge = BorderRadius.all(Radius.circular(24.0));

const kCardBorderRadius = BorderRadius.all(Radius.circular(12.0));

const kDefaultAnimationDuration = Duration(milliseconds: 150);

const kDefaultElevation = 0.0;
