name: zenit_ui_example
description: Example for ZenitUI components.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  zenit_ui:
    path: ../
  gap: ^3.0.0

dev_dependencies:
  flutter_lints: ^2.0.1


flutter:
  uses-material-design: true

  assets:
    - assets/icon_light.png
    - assets/banner.png