name: zenit_ui
description: ZenitUI library.
version: 0.0.8
homepage: dahliaos.io
publish_to: 'none'

environment:
  sdk: ">=2.17.0 <3.0.0"
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  gap: ^2.0.1
  google_fonts: ">=2.3.2 <4.0.0"
  window_manager: 
    git: https://github.com/leanflutter/window_manager.git
  handy_window: ^0.3.1

dev_dependencies:
  dahlia_lint:
    git: https://github.com/larsb24/dahlia_lint.git

flutter:
